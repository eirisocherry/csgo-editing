echo ""
echo ""
echo "─────────────────────────[POV-CONFIG]──────────────────────────"
echo "red     -> highlight current player's kills and hide others (execute this again if the killfeed shows you died; don't use it with the mirv_pov method)"
echo "resetkf -> reset killfeed settings (do this if you accidentally activated 'red' on mirv_pov method)"
echo ""
echo "mirv_fov <fov> -> set the custom fov (ex: mirv_fov 100 -> sets the fov value to 100; DON'T use fov_cs_debug or zooming will be broken)"
echo "Don't forget to type in 'mirv_fov default' when you're done with the pov recording, otherwise the campath's fov will be static"
echo "" 
echo "hud0/hud1/hud2 -> toggle the hud mode (hud0 -> off; hud1 -> on; hud2 -> only killfeed & crosshair)"
echo "crosshair 1/0  -> enable/disable the crosshair (default: disabled)"
echo "[/]            -> toggle left & right player borders"
echo "xplayers/xguns/xall/xoff -> toggle the xray (xplayers -> players; xguns -> guns; xall -> all; xoff -> disable)"
echo ""
echo "list -> view the players' information"
echo "see  -> show the info about the player you're spectating for or the entity you're looking at"
echo "mirv_replace_name <userID> <name> -> replace player's name (ex: mirv_replace_name 75 Rikki -> sets the name for the player with the userID=75 to Rikki)"
echo "mirv_replace_name filter clear    -> clear replacements"
echo ""
echo "POV with the realistic HUD (mirv_pov method):"
echo "1. Get the index (the left one) with the 'list' or 'see' command and type in 'mirv_pov <index>'."
echo "2. Rejoin the demo, activate the 'fe' config, enjoy!"
echo "If you wanna quit the mirv_pov method, then type in 'mirv_pov 0'"
echo "───────────────────────────────────────────────────────────────"
echo ""
echo ""